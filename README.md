# howie-chen

个人向 AI 助手（移动端优先）：**陈科豪体系**短视频与朋友圈能力，对话接 **DeepSeek**，按需 **Tavily** 联网；支持流式输出、本机「个人风格」注入。

## 开发与部署

```bash
npm install
npm run dev
```

全栈本地（含 `/api`）：

```bash
npx vercel dev
```

部署到 **Vercel** 时，在控制台配置环境变量（参见 `.env.example`）。

## 仓库

远程：`git@github.com:ahujack/howie-chen.git`
